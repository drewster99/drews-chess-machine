//
//  DrewsChessMachineTests.swift
//  DrewsChessMachineTests
//
//  This file used to hold the Xcode-generated boilerplate. Real tests
//  live in topic-specific files alongside this one:
//
//   - PolicyEncodingTests.swift    — 76-channel bijection round-trips
//   - BoardEncoderTests.swift      — input-tensor plane invariants
//   - RepetitionTrackingTests.swift — ChessGameEngine repetition counter
//   - ReplayBufferTests.swift      — on-disk format v3 round-trip
//
//  This file is kept as a single placeholder so the test target's
//  `Build Phases → Compile Sources` list always has at least one
//  Swift file even if the others are removed during refactoring.
//

import XCTest

final class DrewsChessMachineTests: XCTestCase {
    func testTargetBuilds() {
        // Trivial smoke test — its passing means the test target was
        // built cleanly. The real assertions live in the topic-specific
        // test files in this same target.
        XCTAssertTrue(true)
    }
}
